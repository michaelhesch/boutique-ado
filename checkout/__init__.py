# Sets default config class for the app.
# Needs to have this do Django knows about custom ready method
# so that signals will work. From signals module.
default_app_config = 'checkout.apps.CheckoutConfig'
